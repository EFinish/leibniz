apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argument-access.fullname" . }}-test-connection"
  labels:
    {{- include "argument-access.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argument-access.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
