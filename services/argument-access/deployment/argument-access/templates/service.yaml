apiVersion: v1
kind: Service
metadata:
  name: {{ include "argument-access.fullname" . }}
  labels:
    {{- include "argument-access.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argument-access.selectorLabels" . | nindent 4 }}
